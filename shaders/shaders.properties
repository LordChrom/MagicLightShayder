iris.features.required = CUSTOM_IMAGES BLOCK_EMISSION_ATTRIBUTE COMPUTE_SHADERS
oldLighting=true

shadow.enabled=true
shadowTranslucent=false
shadowTerrain=true
shadowEntities=false
shadowPlayer=false
shadowBlockEntities=false

skipAllRendering=false
voxelizeLightBlocks=true

occlusion.culling=true

#define VOXELIZATION_SIZE 16
#define VOXELIZATION_SIZE_SECTIONS 32


image.worldVox = worldVoxSampler rgba_integer RGBA8UI unsigned_byte true false VOXELIZATION_SIZE VOXELIZATION_SIZE VOXELIZATION_SIZE_SECTIONS
image.lightVox = lightVoxSampler rgba RGBA16F half_float false false 18 18 18
allowConcurrentCompute = false

const int shadowMapResolution = 0;
const float voxelDistance = 160.0;